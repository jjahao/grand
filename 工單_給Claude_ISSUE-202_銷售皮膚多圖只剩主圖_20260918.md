# 工單給 Claude：ISSUE-202 銷售皮膚商品詳情多圖只剩主圖

> 開單：2026-09-18 Codex（James 回報「之前多張照片可以顯示，現在不行」）
> 優先級：P1 客戶頁回歸
> 地盤：`grand_store/shop-skin.js` 的商品詳情圖片解析；不得碰既有 `renderPrettyGrid`／搜尋／價格／購物車／追蹤邏輯
> 本工單已完成唯讀診斷，尚未改碼、未 commit、未 push、未重新注入正式站

## 一句話結論

照片沒有從 Shop2000 消失。Shop2000 單品原始頁仍有全部照片，但額外照片現在由頁內 `imgstr` JavaScript 動態產生；皮膚用 `fetch()`＋`DOMParser` 讀取單品 HTML，`DOMParser` 不會執行腳本，而現行 `gpParseDetail()` 又只掃靜態 `img[src]`，所以漂亮商品彈窗只剩主圖。

## 2026-09-18 正式站重現證據

正式站：`https://grand.shop2000.com.tw/`
正式皮膚：`https://jjahao.github.io/grand/shop-skin.js?v=20`

測試商品：

- psn：`52488689`
- 名稱：`ROOMMATE 輕巧型手部按摩機HEHDM001`
- 分類：`/product/684454`

實際結果：

1. 會員登入狀態開原始單品頁 `/product/p52488689`：頁面顯示「共 7 張」。執行後 DOM 有：
   - `52488689-5o.jpg`
   - `52488689-1o.jpg`
   - `52488689-2o.jpg`
   - `52488689-3o.jpg`
   - `52488689-4o.jpg`
   - `52488689-6o.jpg`
   - `52488689-7o.jpg`
2. 回分類 `/product/684454`，從銷售皮膚點商品名稱開漂亮彈窗：只顯示 `1 / 1`、1 個縮圖、只有 `52488689-5o.jpg`。
3. 原始單品頁內可見 Shop2000 現行產圖腳本：

```js
var imgstr = '|5||1||2||3||4||6||7|';
noArr = imgstr.split('||');
// showPage() 再把每個序號組成 .../52488689-{序號}o.jpg
```

4. 正式站 v20 與本機 `grand_store/shop-skin.js` SHA-256 完全相同：

```text
5d7793dae31ab93c4fa5755445247c4551e3ba8fdc61f9986f5c5af088d6dc5e
```

因此可排除「照片沒上傳」及「正式站還在跑另一版皮膚」。這是皮膚解析方式與 Shop2000 現行 HTML 格式不相容。

## 真因位置

`grand_store/shop-skin.js:1320-1329`

```js
function gpParseDetail(doc, psn, fallback) {
  var rawImages = [], seenImages = {};
  [].forEach.call(doc.querySelectorAll('img[src]'), function (im) {
    // 只看已存在的 <img src>
  });
}
```

`grand_store/shop-skin.js:1354-1363`

```js
fetch('/product/p' + psn)
  .then(function (r) { return r.text(); })
  .then(function (html) {
    var doc = new DOMParser().parseFromString(html, 'text/html');
    return gpParseDetail(doc, psn, fallback);
  });
```

`DOMParser` 不會執行原始頁的 `showPage()`，所以 `imgstr` 裡另外 6 張不會變成 `<img>`。現行 fallback 最後只保住列表主圖，結果就是 `1 / 1`。

## 指定修法（最小修改，不重設計）

只擴充 `gpParseDetail()` 的圖片來源收集：

1. 保留現有 `img[src]` 掃描與去重。
2. 再掃 `doc.querySelectorAll('script')` 的文字，找 Shop2000 自己的 `var imgstr = '...'`。
3. 只接受由數字、單引號／雙引號、直線與 `||` 組成的既有格式；抽出照片序號並保持 `imgstr` 原順序。
4. 以已確認屬於同一 psn 的主圖 URL 當 base，將每個序號組成 `.../{psn}-{序號}o.jpg`，寫回同一個 `rawImages`／`seenImages`。
5. `imgstr` 找不到或格式不合法時，維持現行靜態圖片／fallback 行為；單圖商品不得壞。

建議把「加入圖片」抽成 1 個小 helper，靜態 `<img>` 與 `imgstr` 共用正規化及去重，避免主圖重複兩次。

### 安全限制

- 禁止 `eval()`、`Function()` 或執行 Shop2000 回傳腳本。
- 禁止用 1～N 逐張 `HEAD`／`GET` 猜圖，避免 Shop2000 限流，也不能把 404 當圖片。
- `imgstr` 只收純數字照片序號，設定合理上限（例如最多 100 張）；異常格式 fail closed。
- 只可從已驗證含 `/{psn}-` 的同商品圖片 URL 推導 base，不得拼別商品／別店圖片。
- 不改商品資料、不補傳照片、不碰後台、不動上架腳本。
- 不碰 `renderPrettyGrid`（仍是 ISSUE-147 既有鎖範圍）、搜尋、會員、價格、購物車、收藏、分類與 CSS 外觀。

## 建議允許修改的檔案

- `grand_store/shop-skin.js`：只限 `gpParseDetail()` 附近的圖片解析
- 可新增 `grand_store/test_issue202_detail_images.mjs` 或等價最小測試檔

除此之外先不要動；若一定要改其他檔，先回報 James。

## 隔離驗收（先做，不能拿正式站當測試場）

至少鎖住以下案例：

1. 七圖 fixture：

```text
imgstr = |5||1||2||3||4||6||7|
期望 = 5o, 1o, 2o, 3o, 4o, 6o, 7o（7 張、順序相同、無重複）
```

2. 主圖本來已是 `5o.jpg`，`imgstr` 第一張也是 5：仍只能出現一次。
3. 單圖 HTML、沒有 `imgstr`：維持 1 張。
4. `imgstr` 空白／破損／夾非數字內容：不得執行、不得猜 URL、維持安全 fallback。
5. 他店或不同 psn 的圖片：不得混入。
6. `node --check grand_store/shop-skin.js` 通過；新測試全綠。

## 正式站驗收（James 同意上線後才做）

1. push `grand_store` 後，確認 GitHub Pages 新檔已更新。
2. 依現行注入流程將皮膚版本由 `v=20` 升到下一版；保留 GA 與原有第三方碼。
3. 會員客人路徑開 `/product/684454`，點 psn `52488689`：
   - 彈窗顯示 `1 / 7`；
   - 有 7 個縮圖；
   - 下一張後顯示 `2 / 7` 且圖片真的變更、`naturalWidth > 0`；
   - 關閉再開仍為 7 張；
   - 手機寬度可點縮圖及左右滑，沒有 console error。
4. 抽一個單圖商品：仍顯示 `1 / 1`，不出現空白縮圖／404。
5. 回歸：搜尋、分類、加入購物車、追蹤清單各跑 1 次，行為不變。

## 完成回報格式

- 真因是否與本工單一致
- 實際修改檔案與行數
- 隔離測試方法＋實際結果
- 是否已 commit／push／重新注入（分開寫）
- 正式站七圖商品與單圖商品的實際結果
- console／network 錯誤
- AI API／模型／成本
- 尚待 James 確認事項

## 2026-09-18 Claude 施工回報

- 真因：與本工單一致。用 James 已登入 Chrome 唯讀確認 `/product/p52488689` 靜態 HTML 只有 `52488689-5.jpg`，其餘 6 張全由 `var imgstr = '|5||1||2||3||4||6||7|'` + `showPage()` 動態插入 `#imgArea0`，且 `showPage()` 內寫死 base `https://img2.shop2000.com.tw/75210/p488/`。
- 修改：`grand_store/shop-skin.js` 1320–1360 附近。新增 `gpDetailAddImage()`（同 psn／o.jpg／去重 helper）、`gpDetailImgstrSeq()`（只接受 `var imgstr = '純數字與|'` 字面值，非數字／超過 100 張／非字面值一律回空）；`gpParseDetail()` 靜態 `img[src]` 改走 helper，列表 fallback 主圖也登記進 seenImages，再以 `rawImages[0]`（必須符合 `^(.*/){psn}-\d+o\.jpg`）當 base 還原 imgstr 序號。未動 renderPrettyGrid／搜尋／價格／購物車／追蹤／CSS。
- 隔離測試：`node grand_store/test_issue202_detail_images.mjs`（Playwright 無頭 Chromium + 真 DOMParser，抽真實函式源碼）8 組全綠：七圖順序無重複／主圖重複去重／單圖無 imgstr／單圖 `|1|`／空白·破損·夾非數字·路徑·101 張 fail closed／非 var 字面值不採用／他店 psn 不混入不當 base／無圖無 fallback 仍空陣列。
- 正式站唯讀對照（未注入、未改站）：以修後解析跑真實 HTML → `52488689` old=1 → new=7（5o,1o,2o,3o,4o,6o,7o，7 張皆 500×500 可載入）；`56778002` 1→1；`56351273` 1→1；`55882791` 1→2。
- commit：grand_store 內本機 commit；**未 push、未升 `inject_css.mjs` 的 `v=20`**，等 James 同意後才做正式站驗收（工單「正式站驗收」段）。
- AI API：無（本機規則＋Playwright）；模型 Claude Opus 5（本 session）。
