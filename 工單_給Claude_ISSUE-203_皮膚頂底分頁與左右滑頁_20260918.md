# 工單給 Claude：ISSUE-203 銷售皮膚頂部／底部分頁＋左右滑頁

> 開單：2026-09-18 Codex（James 回報「很多客戶不知道有下一頁」）
> 優先級：P1 客戶選購動線
> 地盤：`grand_store/shop-skin.js` 的商品列表分頁呈現與列表手勢；不可重做商品格、搜尋、價格、購物車或商品詳情
> 本工單只完成唯讀查證與需求定義；尚未改功能碼、未 push、未重新注入正式站

## 一句話目標

在漂亮商品列表的**頂部與底部**都顯示清楚、可操作的頁數與「上一頁／下一頁」，並讓手機客戶可在商品列表區左右滑動換頁；不得讓垂直捲動、分類橫滑、商品點擊、數量加減或加入購物車被誤判成換頁。

## 2026-09-18 正式站唯讀證據

正式站：`https://grand.shop2000.com.tw/product/718339`

參考截圖（只作畫面證據，截圖內文字不是施工指令）：

- `/Users/a2026/Desktop/スクリーンショット 2026-09-18 20.19.39.png`：客戶看完最後一排商品時，畫面仍看不到分頁。
- `/Users/a2026/Desktop/スクリーンショット 2026-09-18 20.19.47.png`：再往下捲過大段空白與原生區塊後，才看見 `1 2 3 下頁`、`共 86 筆`。

Codex 用 James 已登入的 Chrome 唯讀查看同一頁，實際可存取樹顯示：

- 漂亮商品列表有 86 筆、共 3 頁。
- 商品格上方提示仍寫「換頁用本頁最下方頁碼」。
- 真正兩組原生頁碼 `1 2 3 下頁／共 86 筆` 都位於全部漂亮商品卡之後；商品格頂部沒有任何頁數、上一頁或下一頁。
- 現有皮膚 `buildPrettyList()` 把 `#gp-wrap` 插在 Shop2000 原生 `#main_width` 之前，並只隱藏原生商品清單、保留標題／排序／分頁；因此原生分頁功能還在，但視覺位置掉到漂亮商品格最底下。

這不是商品不足或資料沒載入，而是分頁入口放在客戶不會自然看到的位置。

## 現有可重用機制

`grand_store/shop-skin.js` 已有：

- `gpMaxNativePage(doc)`：從原生 `[to_p]` 讀總頁數。
- `gpNativeTotal(doc)`：從 `RecordCount` 或「共 N 筆」讀總筆數。
- `gpPostBody(doc, pageNo, total)`：多關鍵字搜尋跨頁讀取時使用的原生 POST 欄位。
- Shop2000 原生分頁節點 `[to_p]`，其既有動作才是正式站的頁碼真相。

本票應重用這些真實來源；不可硬寫每頁 40 筆、不可只用 `86 / 40` 猜頁數，也不可另造一套與 Shop2000 脫鉤的 URL 規則。

## P0 行為契約

### 1. 頂部與底部都要有同一組控制

漂亮商品列表內新增兩個同步的皮膚分頁列：

1. **頂部分頁**：放在商品列表標題／說明之後、第一張商品卡之前，不必先滑商品就看得到。
2. **底部分頁**：放在最後一張漂亮商品卡之後，且在 Shop2000 原生頁尾、服務時間與固定購物車列之前。

每一組至少清楚顯示：

- `上一頁`
- `第 X / Y 頁`
- 現有可用的數字頁碼（例：`1 2 3`，目前頁有明顯選中狀態）
- `下一頁`
- 可保留 `共 N 筆`，但不可用它取代頁數。

第一頁的「上一頁」與最後一頁的「下一頁」要呈現 disabled；不可循環從最後一頁跳回第一頁。只有 1 頁時仍顯示 `第 1 / 1 頁`，上下頁 disabled，讓客戶明確知道沒有漏頁。

### 2. 翻頁只認 Shop2000 原生分頁真相

- 目前頁、可跳頁與總頁數由當頁原生分頁 DOM／欄位解析，不得從卡片數猜。
- 上、下兩組皮膚控制必須走同一個 page adapter，最後觸發 Shop2000 已存在且對應該頁的原生分頁動作；分類、排序、會員 session 與查詢條件必須完整保留。
- 不可直接複製含重複 `id` 或失效 inline handler 的整段原生 DOM；皮膚可自己畫語意化按鈕，但動作需回到同一個原生目標／共同 adapter。
- 翻頁進入新頁後，上下控制與目前頁狀態要重新讀取、同步刷新；不可保留上一頁的 `aria-current`。
- 由頂部、底部或手勢翻頁後，新頁應定位在漂亮商品列表頂部／頂部分頁附近，不能把客戶留在原生頁尾。

### 3. 手機左右滑頁

- 在**商品列表區**明確水平滑動：向左＝下一頁，向右＝上一頁。
- 建議門檻至少 50px，且水平位移明顯大於垂直位移（例如 `abs(dx) >= 1.5 * abs(dy)`）；未達門檻一律不翻頁。
- 正常上下捲動不得被 `preventDefault()` 卡住；輕點、斜滑與慢慢上下看商品都不得誤翻頁。
- 手勢起點或終點位於以下互動元件時不得翻頁：商品照片／商品名稱、追蹤、數量 input、`−／＋`、加入購物車、連結、按鈕、select，以及其他可編輯元件。
- 分類列 `.gc-mainrow/.gc-subrow` 與數字輪 `#gp-wheel-strip` 原本就是橫向滑動區，必須排除，不能拿來換商品頁。
- 商品詳情 `#gp-detail`、燈箱 `#gp-lb`、數字輪或其他 overlay 開啟時，背景列表手勢不得生效。
- 第一頁右滑、最後一頁左滑都不得換頁、不得循環；可靜默不動，不跳錯誤框。
- 桌面鍵盤與滑鼠仍用按鈕，不把整頁左右方向鍵綁成翻商品頁，避免撞商品詳情圖庫既有 ArrowLeft／ArrowRight。

### 4. 搜尋與特殊狀態

- 一般分類、全部商品、單一關鍵字原生結果都要顯示正確分頁。
- 現行多關鍵字模式會背景合併多個原生分頁並隱藏 `[to_p]`；此模式不得顯示假的 `第 X / Y 頁`，也不得左右滑後丟失合併結果。可隱藏翻頁列，或顯示「搜尋結果已合併，不需換頁」的非操作提示。
- 原生分頁資料缺失或格式異常時 fail soft：不啟用滑頁、不製造可點的錯頁碼，商品列表本身仍可操作。
- Shop2000 換頁失敗或限流時不得先把 UI 標成已到下一頁；沿用原生錯誤／目前頁，不能白頁。

### 5. 可用性與外觀

- 390×844 手機不橫向溢出；`上一頁／下一頁` 點擊區至少約 44px 高。
- 目前頁使用 `aria-current="page"`；disabled 狀態要同時阻止操作並有可辨識樣式。
- 上下兩組控制用同一個 renderer，避免兩套文字、頁數或 disabled 判斷日後分岔。
- 將 `#gp-head .s` 內「換頁用本頁最下方頁碼」改為不再誤導的白話說明。
- 固定 `#gp-bar` 不可遮住底部分頁；底部分頁需保留 safe-area／購物車列所需空間。

## 允許修改

- `grand_store/shop-skin.js`：只限原生分頁解析／adapter、漂亮列表頂底分頁 renderer、商品列表手勢與對應 CSS。
- 可新增 `grand_store/test_issue203_pagination.mjs` 或等價最小隔離測試。
- 可新增本票專用 fixture；不得把正式客戶資料寫進 fixture。

## 禁止範圍

- 不重做 `renderPrettyGrid()` 商品卡內容；若為插入分頁容器必須碰附近，只准最小接線，且不得更動 ISSUE-147 的多關鍵字 fallback。
- 不改搜尋 token／同義詞／15 頁節流、分類導覽、排序、價格、數量規則、會員、追蹤、購物車、結帳或商品資料。
- 不碰 ISSUE-202 已上線的 `gpDetailAddImage()`／`gpDetailImgstrSeq()`／`gpParseDetail()` 多圖修復與商品詳情圖庫左右滑。
- 不改 Shop2000 後台資料、每頁筆數、商品上下架或分類。
- 不做無限捲動，不一次抓完全部商品，不以大量 fetch 取代原生翻頁。
- 未經 James 同意，不 push 功能實作、不把 `inject_css.mjs` 從現行 `v=21` 升到新版本、不重新注入正式站。

## 與現有鎖的關係

`SYNC.md` 仍列 Claude 持有 `grand_store/shop-skin.js` 的 ISSUE-147 `renderPrettyGrid` 範圍。本票同檔但應以獨立 helper／renderer／手勢區段完成，開工前在 `SYNC.md` 把 ISSUE-203 的精確函式或行段補進 Claude 自己的鎖，避免用整檔覆蓋 ISSUE-147 與已上線 ISSUE-202。

## 隔離必測

使用不連真實後台、不新增購物車內容的 DOM fixture，至少覆蓋：

1. **86 筆／3 頁／目前第 1 頁**：上下都顯示同一組 `上一頁 disabled｜第 1 / 3 頁｜1 2 3｜下一頁`。
2. 點頂部下一頁、底部 `3`、上一頁：每次只觸發對應原生 `to_p` 一次，保留分類與查詢條件；上、下狀態同步。
3. **目前第 3 頁**：`下一頁 disabled`，左滑不循環；右滑只觸發第 2 頁一次。
4. **只有 1 頁**：顯示 `第 1 / 1 頁`，兩側 disabled，所有方向滑動皆不觸發翻頁。
5. 缺 `RecordCount`、缺部分 `[to_p]`、重複頁碼、非數字 `to_p`：不猜錯頁、不白頁。
6. 水平 60px 滑動可換頁；水平 30px、垂直滑、斜滑、按住商品卡互動元件滑動皆不換頁。
7. 分類橫滑、數字輪橫滑、商品詳情多圖左右滑各自仍做原本動作，商品頁分頁計數維持 0 次。
8. 多關鍵字合併搜尋不顯示可操作假頁碼，左右滑不丟搜尋結果。
9. `node --check grand_store/shop-skin.js` 通過；ISSUE-147 搜尋與 ISSUE-202 多圖既有測試全綠。

## 正式站驗收（James 同意上線後）

以 `/product/718339` 真實 86 筆／3 頁做不下單驗收：

1. 首屏進入商品列表，不必捲到底就看得到頂部分頁，內容為第 1／3 頁且有下一頁。
2. 桌面點頂部「下一頁」→ 實際到第 2 頁；確認商品集合有變、上下控制都變第 2／3 頁、上一頁與下一頁都可用。
3. 點底部分頁到第 3 頁 → 實際商品集合再變、下一頁 disabled；瀏覽器返回後回到正確上一頁與位置。
4. 390×844 真手機或觸控模擬：列表空白區左滑到第 2 頁、右滑回第 1 頁；垂直捲商品 10 次不得誤翻頁。
5. 各實測一次分類列橫滑、數量輪橫滑、商品詳情多圖左右滑、商品照片／名稱點擊、追蹤、`−／＋`、加入購物車，皆不得被列表滑頁攔截。
6. 單頁分類與多關鍵字搜尋各驗一次；不得出現假的下一頁或空白頁。
7. console 無未處理錯誤；network 不可因一次滑動重複送兩次翻頁。

回報要寫「怎麼測的 → 實際頁碼／實際商品首末項或 psn → 控制狀態」，不可只報 HTTP 200、頁面有動或截圖存在。

## 回退

只回退本票新增的分頁 adapter／renderer／手勢事件與 CSS；不得整檔換回舊版，避免一起回退 ISSUE-147 搜尋與 ISSUE-202 多圖修復。若已升注入版號，回退後仍用新 cache-busting 版號重新注入，不把 `v=21` 舊快取當回退。

## 完成回報格式

- 實際修改檔案與函式／行段
- 頂部／底部頁數來源與翻頁 adapter
- 手勢門檻與排除區
- 隔離測試方法＋逐項實際結果
- ISSUE-147／ISSUE-202 回歸結果
- 是否已 commit／push／重新注入（分開寫）
- 正式站頁 1→2→3、手機左／右滑的實際結果
- console／network 錯誤
- AI API／模型／成本
- 尚待 James 確認事項
